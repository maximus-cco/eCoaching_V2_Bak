$(function () {
	// Moved to Index.cshmtl
	// Otherwise it gets registered multiple times (the number of times the modal show/close)
	//// onclick "Is the coaching opportunity a confirmed Customer Service Escalation (CSE)?"
	//$('body').on('change', '#IsCse', function () {
	//	if ($(this).val() === 'true') {
	//		alert($(this).val());
	//	}
	//	else {
	//		alert($(this).val());
	//	}
	//});



	//$('body').on('click', '#btn-submit', function (e) {
	//	e.preventDefault();
	//	if (e.handled !== true) {
	//		e.handled = true;
	//		submitReview(reviewUrl, myTable, $('#div-my-pending-text'), 'The log has been successfully updated.', 'Failed to update the log.');
	//	}
	//});



})